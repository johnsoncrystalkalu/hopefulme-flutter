 flutter run -d chrome
 flutter run -d emulator-5554
 
flutter run -d 27be0f2b

flutter run --profile -d 27be0f2b
 or
Press:
flutter emulators --launch Pixel_9_Pro_XL_API_34_-app
Ctrl + Shift + P

Then type:

Flutter: Launch Emulator

on andriod:
php artisan serve --host=0.0.0.0 --port=8000

flutter run --dart-define=API_BASE_URL=http://10.142.136.163:8000/api
flutter run -d 27be0f2b --dart-define=API_BASE_URL=http://10.142.136.163:8000/api


flutter build apk --release

flutter run -d 27be0f2b

futter build apk --debug
flutter build apk --release


version: 1.0.0+1

current play store
verson code: 40
version name 7.4


New app to be uploaded
version code: 50
version name 10.0.1

"C:\Users\Johnson Crystal Kalu\Desktop\flutter\projects\hopefulme-upload-certificate.pem"
"C:\Users\Johnson Crystal Kalu\Desktop\flutter\projects\hopefulme-upload-key.jks"
"C:\Users\Johnson Crystal Kalu\Desktop\flutter\projects\keystore.jks"


## Flutter app checklist


### 3) Secrets/token safety
- ⚠️ **No obvious secure storage package in `pubspec.yaml`** (verify if custom secure token storage exists internally)
- ✅ **No hardcoded backend admin keys seen in checked files**

### 5) Permission UX
- ✅ **Permission descriptions present on iOS (`Info.plist`)**
- ⚠️ **Need “denied later” flow check in app UX (open settings + retry path per feature)**

### 6) Error/log safety
- ⚠️ **Need final audit to ensure no sensitive logs/toasts in release**
- ✅ Your API client already has user-friendly error fallbacks


## Highest-priority fixes (do first)

1. **Set `usesCleartextTraffic` to false for release** (or remove it).
2. **Enable Android release shrinking/obfuscation** (`minifyEnabled`, `shrinkResources`, ProGuard rules).
3. **Build release with Flutter obfuscation** (`--obfuscate --split-debug-info=...`).
4. **Implement secure token storage verification** (Keystore/Keychain).
5. **Add integrity checks** (Play Integrity, App Attest/DeviceCheck).
6. **Add robust permission re-prompt flow** (feature-triggered, settings deep-link).
7. **Add global rate limits + anomaly monitoring on Laravel**.

You add it once in your build config as a Dart define:

--dart-define=IOS_APP_STORE_ID=1234567890

flutter clean
flutter pub get
flutter build appbundle --release

flutter build appbundle --release --obfuscate --split-debug-info=build/symbols
flutter build appbundle --release --shrink